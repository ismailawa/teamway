describe('The ShiftsService', () => {
  beforeAll(() => {});

  describe('when creating a Shift', () => {
    it('should return a Shift', async () => {
      // arrange

      //act

      // assert
      expect(true).toEqual(true);
    });
  });
});
